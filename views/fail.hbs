<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>fail</title>
    <style>
      body { background-image: url("/images/3.svg"); background-repeat:
      no-repeat; background-size: cover; } h1 { color: #333; text-align: center;
      } .form{ margin-top: 25%; } button { display: block; margin: 20px auto;
      padding: 10px 20px; border: none; border-radius: 4px; background-color:
      #007bff; color: #fff; font-size: 2em; } @media (max-width: 600px) { button
      { width: 80%; } h1 { font-size: 1.2em; } }

    </style>
  </head>
  <body>
    <div class="form">
      <h1>Mã Đơn Hàng: {{orderCode}} </h1>
            <button id="btn-local" type="button">Tiếp tục thanh toán</button>
    </div>
    <script>
      const url = localStorage.getItem("currentPaymentUrl");
      console.log(url);
       const btn = document.getElementById("btn-local"); btn.addEventListener("click", () => {
      window.location.href = url; }); 
      fetch("/api/respayment?orderCode=" + "{{orderCode}}" + "&id=" + "{{id}}" + "&status=" + "{{status}}", {
  method: "POST",
  headers: {
    "Content-Type": "application/json"
  }
})
.then(res => {
  if(res.status === 200) {
    console.log("success");
  } else {  
    console.log("fail");
  }
})
.catch(err => {
  console.log(err);
})

    </script>
  </body>
</html>